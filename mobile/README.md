SOS Admin (Expo)

This is a small Expo-based admin dashboard for the SOS Command Center.

Run:

```powershell
cd mobile
npm install
npm run start
```

Open the project in Expo Go or a simulator. Data is static and lives in `src/data`.
